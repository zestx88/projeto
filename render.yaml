services:
  - type: web
    name: tadashi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node seed.js && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
